package ru.hotdog.multicam

import org.junit.Test

import org.junit.Assert.*

// Проверяет пример локального unit-теста.
class ExampleUnitTest {
    // Проверяет корректность простого сложения.
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}